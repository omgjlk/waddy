# Work request: {{title}}

You're picking up a unit of work handed off from another session. This briefing
is self-contained — you don't need any tools or context from the handing-off
agent to do the work or to report back.

## Goal

{{goal}}

## Context / links

{{#each links}}
- {{this}}
{{/each}}

## Where to work

Repo / directory: `{{repo}}`

## Reporting back (the only handoff step)

When you finish — or if you get blocked — **append** the block below to the END
of this file (append-only; do not edit or delete anything already in it):

    {{notes_path}}

Block to append (fill in the fields; use your own Copilot session id and an
ISO-8601 timestamp):

```
## Handoff <ISO-8601-timestamp> — session <your-session-id>
- state: ready-to-close | blocked | in-progress
- did: <concise summary of what you accomplished>
- artifacts: <PR/issue refs and URLs, comma-separated, or "none">
- for-waddy: <what you want done next — e.g. "close when PR #NN merges">
```

Field notes:
- `state`: `ready-to-close` (done — name the artifacts so they can be verified),
  `blocked` (stopped on a dependency — name it in `for-waddy`), or `in-progress`
  (checkpoint; more coming).
- Be specific in `artifacts` — the tracking agent verifies them before closing.

That's it. A tracking agent ("waddy") reads that file later and closes the work
out. No other reporting is needed.

---
*Work request generated by [waddy](https://github.com/omgjlk/waddy) — task `{{task_id}}`*
